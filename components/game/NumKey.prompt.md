Large round key for the learner PIN / numeric keypad; tactile press like Button.

```jsx
<NumKey tone="green" onClick={() => press(1)}>1</NumKey>
```

Tones: green (default), blue, light. `size` is the diameter (60px standard). Lay out 0–9 in a CSS grid with `gap`.
