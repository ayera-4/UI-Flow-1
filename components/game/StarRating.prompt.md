Row of stars for level scores — three-star scheme by default (matches level-select and end-of-level screens).

```jsx
<StarRating value={1} max={3} size={28} />
```

`value` filled count, `max` total, `size` px per star, `tone` `star` (gold) or `purple` (advanced tier). Display-only.
